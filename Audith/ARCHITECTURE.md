# 🎯 PROFILE SYSTEM ARCHITECTURE

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        KIOS APPLICATION                          │
└─────────────────────────────────────────────────────────────────┘
                              ↓
        ┌───────────────────────┬───────────────────────┐
        │                       │                       │
        ↓                       ↓                       ↓
    ┌────────┐           ┌──────────┐           ┌────────────┐
    │ LOGIN  │           │ NAVBAR   │           │  SIDEBAR   │
    │ PAGE   │           │ PROFILE  │           │   MENU     │
    │        │           │ BUTTON   │           │            │
    └────────┘           └──────────┘           └────────────┘
        ↓                       │                       │
        │                       │                       │
        │                  onclick="                onclick="
        │              showProfileModal()"      showSection(
        │                       │                  'profile')"
        │                       ↓                       ↓
        │                ┌──────────────┐       ┌──────────────┐
        │                │ PROFILE      │       │ showSection  │
        │                │ MODAL        │       │ ('profile')  │
        │                │              │       │              │
        │                │ • Display    │       │ • Hide all   │
        │                │ • Modal      │       │ • Show prof  │
        │                │ • Quick view │       │ • Active nav │
        │                │              │       │ • loadProf   │
        │                └──────────────┘       │ Page()       │
        │                       │               └──────────────┘
        │                       │                      ↓
        │                [Buka Profil]         ┌──────────────┐
        │                Lengkap Button         │loadProfilePage
        │                       │               │              │
        │                       └─────────────→ │ • Get user   │
        │                        goToProfile    │ • Get last   │
        │                        Section()      │   login      │
        │                                       │ • Populate   │
        │                                       │   elements   │
        └──────────────────────────────────────→└──────────────┘
                                                       ↓
                                              ┌──────────────────┐
                                              │ PROFILE SECTION  │
                                              │                  │
                                              │ ┌──────────────┐ │
                                              │ │ Profile Card │ │
                                              │ ├──────────────┤ │
                                              │ │ Account Info │ │
                                              │ ├──────────────┤ │
                                              │ │Change Pass   │ │
                                              │ │ • Input curr │ │
                                              │ │ • Input new  │ │
                                              │ │ • Confirm    │ │
                                              │ │ • [Update]   │ │
                                              │ └──────────────┘ │
                                              └──────────────────┘
                                                       ↓
                                              sectionUpdate
                                              Password()
                                                       ↓
                                              ┌──────────────────┐
                                              │ VALIDATION LAYER │
                                              │                  │
                                              │ ✓ Fields filled  │
                                              │ ✓ Length >= 6    │
                                              │ ✓ Match confirm  │
                                              │ ✓ Curr correct   │
                                              └──────────────────┘
                                                  ✓ Pass ↓
                                              ┌──────────────────┐
                                              │ UPDATE PASSWORD  │
                                              │                  │
                                              │ demoUsers[id]    │
                                              │ .password        │
                                              └──────────────────┘
                                                       ↓
                                              ┌──────────────────┐
                                              │ SUCCESS ALERT    │
                                              │ Reset Form       │
                                              │ Hide Alert (3s)  │
                                              └──────────────────┘
```

---

## Data Flow Diagram

```
┌──────────────────────┐
│   USER LOGIN         │
│                      │
│ username: admin      │
│ password: admin123   │
└──────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ Validate in demoUsers Array          │
│ ├─ Find user by username             │
│ ├─ Check password match              │
│ └─ If match, create currentUser obj  │
└──────────────────────────────────────┘
         ↓ ✓ Success
┌──────────────────────────────────────┐
│ Save currentUser to localStorage     │
│ localStorage['currentUser'] = {       │
│   id: 1,                             │
│   name: "Admin User",                │
│   username: "admin",                 │
│   role: "Admin",                     │
│   position: "System Admin"           │
│ }                                    │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ Save Last Login Timestamp            │
│ localStorage['lastLogin_1'] =        │
│   "2025-01-15T14:30:00.000Z"        │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ User Navigate to Profile Section     │
│ Click "Profil" Menu                  │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ loadProfilePage() Called             │
│                                      │
│ 1. Get currentUser from memory       │
│ 2. Populate display elements:        │
│    - sectionProfileName              │
│    - sectionProfileUsername          │
│    - sectionProfileRole              │
│    - sectionProfilePosition          │
│                                      │
│ 3. Get lastLogin from localStorage   │
│ 4. Format to Indonesia locale        │
│ 5. Display formatted date            │
│                                      │
│ 6. Reset password form               │
│ 7. Set hidden username field         │
│ 8. Hide alert messages               │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ PROFILE SECTION DISPLAYED            │
│                                      │
│ ┌─────────────┐ ┌─────────────────┐ │
│ │ Profile     │ │ Account Info:   │ │
│ │ Card:       │ │                 │ │
│ │             │ │ Name: Admin User│ │
│ │ ╭─────────╮ │ │ User: admin     │ │
│ │ │ [Icon]  │ │ │ Role: Admin     │ │
│ │ │ Admin   │ │ │ Login: 14:30:00 │ │
│ │ │ User    │ │ │                 │ │
│ │ ╰─────────╯ │ │ Change Password:│ │
│ │ Admin       │ │ [Fill form...]  │ │
│ └─────────────┘ └─────────────────┘ │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ User Changes Password                │
│                                      │
│ Current: admin123                    │
│ New: newpass123                      │
│ Confirm: newpass123                  │
│ [Perbarui Password]                  │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ sectionUpdatePassword() Validates    │
│                                      │
│ ✓ All fields filled                  │
│ ✓ Password >= 6 chars                │
│ ✓ New == Confirm                     │
│ ✓ Current in demoUsers               │
└──────────────────────────────────────┘
         ↓ ✓ All Valid
┌──────────────────────────────────────┐
│ Update Password in demoUsers         │
│                                      │
│ demoUsers[0] = {                     │
│   ...                                │
│   password: "newpass123"  ← Updated  │
│ }                                    │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ Show Success Alert                   │
│ "✓ Password berhasil diubah!"        │
│                                      │
│ Auto-hide after 3 seconds            │
│ Reset form (clear inputs)            │
└──────────────────────────────────────┘
         ↓
┌──────────────────────────────────────┐
│ User Logout & Login Again            │
│ username: admin                      │
│ password: newpass123  ← New password │
│                                      │
│ ✓ Login Success!                     │
└──────────────────────────────────────┘
```

---

## Component Interaction Diagram

```
                    ┌─────────────────┐
                    │  BROWSER DOM    │
                    └────────┬────────┘
                             ↓
        ┌────────────────────┴────────────────────┐
        │                                         │
        ↓                                         ↓
    ┌────────────┐                         ┌─────────────┐
    │   HTML     │                         │ JAVASCRIPT  │
    │ Elements   │                         │  Functions  │
    ├────────────┤                         ├─────────────┤
    │ <section   │                         │             │
    │  id="prof" │◄─────────────────────┐  │ loadProfile │
    │  class=    │                      │  │ Page()      │
    │  d-none>   │                      │  │             │
    │            │                      │  │ Updates:    │
    │ • Profile  │                      │  │ • Text      │
    │   Card     │                      │  │ • innerHTML │
    │ • Account  │                      │  │ • values    │
    │   Info     │                      │  │             │
    │ • Password │                      │  ├─────────────┤
    │   Form     │                      │  │ section     │
    │            │                      │  │ Update      │
    │ • Alert    │                      │  │ Password()  │
    │   Messages │                      │  │             │
    │            │                      │  │ Validates:  │
    │ • Buttons  │                      │  │ • Fields    │
    │            │                      │  │ • Length    │
    └────────────┘                      │  │ • Match     │
         ▲                              │  │ • Current   │
         │                              │  └─────────────┘
         │                              │
    onclick="                           │
    section                             │
    Update                    Populate data
    Password                           │
    ()"                                │
         │                              │
    ┌────┴───────────────────────────┬─┘
    │                                │
    ↓                                ↓
┌────────────────┐         ┌──────────────────┐
│ USER ACTIONS   │         │ DATA SOURCES     │
│                │         │                  │
│ • Input pass   │         │ currentUser obj  │
│ • Click button │         │ demoUsers array  │
│ • Reset form   │         │ localStorage     │
└────────────────┘         └──────────────────┘
```

---

## State Management Diagram

```
PAGE LOAD
   ↓
┌──────────────────────────────────┐
│ Initialize Auth                  │
│                                  │
│ Get currentUser from localStorage│
│ if not exist → redirect to login │
│ if exist → set window.currentUser│
└──────────────────────────────────┘
   ↓
┌──────────────────────────────────┐
│ LOGGED IN STATE                  │
│                                  │
│ window.currentUser = {           │
│   id, name, username,            │
│   role, position, password       │
│ }                                │
│                                  │
│ Available Sections:              │
│ • Dashboard                      │
│ • Barang                         │
│ • Kasir                          │
│ • Transaksi                      │
│ • Laporan                        │
│ • Pengaturan                     │
│ • PROFILE (NEW) ✨              │
└──────────────────────────────────┘
   ↓
┌──────────────────────────────────┐
│ Click "Profil" Menu              │
│ showSection('profile')            │
└──────────────────────────────────┘
   ↓
┌──────────────────────────────────┐
│ PROFILE SECTION ACTIVE           │
│                                  │
│ Display State:                   │
│ • Show profile section           │
│ • Hide other sections            │
│ • loadProfilePage() triggered    │
│                                  │
│ Data State:                      │
│ • currentUser loaded in view     │
│ • lastLogin retrieved            │
│ • Form reset                     │
│ • Alerts hidden                  │
└──────────────────────────────────┘
   ↓
┌──────────────────────────────────┐
│ USER FILLS PASSWORD FORM         │
│                                  │
│ Form State:                      │
│ • sectionCurrentPassword = "..." │
│ • sectionNewPasswordField = "..."│
│ • sectionConfirmNewPassword = "..
└──────────────────────────────────┘
   ↓
┌──────────────────────────────────┐
│ USER CLICKS "PERBARUI PASSWORD"  │
│ sectionUpdatePassword()           │
└──────────────────────────────────┘
   ↓
┌──────────────────────────────────┐
│ VALIDATION CHECKS                │
│                                  │
│ If any fail → Show Error Alert   │
│              → Stop execution    │
│ If all pass → Continue           │
└──────────────────────────────────┘
   ↓
┌──────────────────────────────────┐
│ UPDATE STATES                    │
│                                  │
│ demoUsers[currentUserId]         │
│ .password = newPassword          │
│                                  │
│ currentUser.password = newPassword│
│                                  │
│ Show success alert               │
│ Reset form inputs                │
│ Hide alert (after 3s)            │
└──────────────────────────────────┘
   ↓
┌──────────────────────────────────┐
│ READY FOR NEXT ACTION            │
│                                  │
│ Can:                             │
│ • Change password again          │
│ • Go to other section            │
│ • Logout & login                 │
└──────────────────────────────────┘
```

---

## Function Execution Flow

```
USER INTERACTION
        ↓
onclick="loadProfilePage()"
        ↓
┌─────────────────────────────────────┐
│ function loadProfilePage()          │
│                                     │
│ try {                               │
│   ├─ Check currentUser exists       │
│   ├─ Get elements by ID             │
│   │                                 │
│   ├─ Populate display elements:     │
│   │  ├─ name → .textContent         │
│   │  ├─ username → .textContent     │
│   │  ├─ role → .textContent         │
│   │  └─ position → .textContent     │
│   │                                 │
│   ├─ Get lastLogin from LS:         │
│   │  ├─ localStorage.getItem()      │
│   │  ├─ new Date(iso)               │
│   │  ├─ toLocaleString(id-ID)       │
│   │  └─ .textContent = formatted    │
│   │                                 │
│   ├─ Reset form:                    │
│   │  ├─ form.reset()                │
│   │  ├─ Set hidden username         │
│   │  └─ Hide alerts                 │
│   │                                 │
│   └─ console.log('success')         │
│ }                                   │
│ catch (error) {                     │
│   └─ console.error(error)           │
│ }                                   │
└─────────────────────────────────────┘
        ↓
PROFILE SECTION POPULATED & READY
```

---

## Integration Points Summary

```
┌──────────────────────────────────────────────────────────────┐
│                    KIOS SYSTEM                               │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  [1] SIDEBAR MENU                                           │
│      └─→ "Profil" item                                      │
│          └─→ onclick="showSection('profile')"               │
│                                                              │
│  [2] showSection() FUNCTION                                 │
│      └─→ if (id === 'profile') loadProfilePage()            │
│                                                              │
│  [3] loadProfilePage() FUNCTION                             │
│      └─→ Populate all section elements                      │
│          └─→ Get currentUser from memory                    │
│          └─→ Get lastLogin from localStorage                │
│          └─→ Display data                                   │
│                                                              │
│  [4] PROFILE SECTION HTML                                   │
│      ├─→ Display area (read-only)                           │
│      └─→ Password form (interactive)                        │
│          └─→ sectionUpdatePassword() on submit              │
│                                                              │
│  [5] PASSWORD UPDATE FUNCTION                               │
│      ├─→ Validate inputs                                    │
│      ├─→ Update demoUsers array                             │
│      ├─→ Show success/error alert                           │
│      └─→ Reset form                                         │
│                                                              │
│  [6] NAVBAR MODAL BUTTON                                    │
│      └─→ onclick="goToProfileSection()"                     │
│          └─→ Close modal                                    │
│          └─→ Navigate to section                            │
│          └─→ Call loadProfilePage()                         │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

This architecture ensures:
✅ Clean separation of concerns
✅ Single source of truth (currentUser)
✅ Reusable components & functions
✅ Easy to test & maintain
✅ Scalable for future additions
